            <h3 class="section-header">General Configuration</h3>

      <label>Prefix</label>

      <h3 class="prefix" id="prefix" type="text" value='<%= db.get(`prefix_${guild.id}`) || config.bot.prefix %>'</h3>

      <button class="btn">Save </button>